---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "Resource hush_confluence_integration - terraform-provider-hush"
subcategory: ""
description: |-
  Manages a Hush Security Confluence integration for scanning Confluence spaces for secrets and sensitive data.
---

# Resource (hush_confluence_integration)

Manages a Hush Security Confluence integration for scanning Confluence spaces for secrets and sensitive data.

## Example Usage

```terraform
# Manage a Confluence integration
resource "hush_confluence_integration" "example" {
  name       = "my-confluence"
  org_domain = "mycompany.atlassian.net"
  user       = "admin@mycompany.com"
  api_key    = var.confluence_api_key
}

# With write-only API key (recommended for production)
resource "hush_confluence_integration" "secure" {
  name              = "my-confluence-secure"
  org_domain        = "mycompany.atlassian.net"
  user              = "admin@mycompany.com"
  api_key_wo        = var.confluence_api_key
  api_key_wo_version = "v1"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the Confluence integration
- `org_domain` (String) The Confluence organization domain (e.g., mycompany.atlassian.net)
- `user` (String) The email address of the Confluence user for authentication

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `api_key` (String, Sensitive) The API key for Confluence authentication
- `api_key_wo` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) The API key for Confluence authentication (write-only). This is more secure than `api_key` because Terraform will not store this value in the state file. Either `api_key` or `api_key_wo` must be specified.
- `api_key_wo_version` (String) Used to trigger updates for `api_key_wo`. This value should be changed when the API key changes. Can be any value (e.g., a timestamp, version number, or hash).
- `description` (String) The description of the Confluence integration

### Read-Only

- `id` (String) The unique identifier of the Confluence integration
- `status` (String) The current status of the integration
