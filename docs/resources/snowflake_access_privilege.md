---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "Resource hush_snowflake_access_privilege - terraform-provider-hush"
subcategory: ""
description: |-
  Manage Snowflake access privileges in the Hush Security platform.
---

# Resource (hush_snowflake_access_privilege)

Manage Snowflake access privileges in the Hush Security platform.

## Example Usage

```terraform
# Create a Snowflake access privilege with read-only access
resource "hush_snowflake_access_privilege" "example" {
  name        = "app-read-only"
  description = "Read-only access to application tables"

  grants {
    privileges    = ["USAGE"]
    resource_type = "database"
  }

  grants {
    privileges    = ["USAGE"]
    resource_type = "schema"
  }

  grants {
    privileges    = ["SELECT"]
    resource_type = "table"
  }

  grants {
    privileges    = ["USAGE"]
    resource_type = "warehouse"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `grants` (Block List, Min: 1) The list of privilege grants (see [below for nested schema](#nestedblock--grants))
- `name` (String) The name of the Snowflake access privilege

### Optional

- `description` (String) The description of the Snowflake access privilege

### Read-Only

- `id` (String) The unique identifier of the Snowflake access privilege
- `type` (String) The type of access privilege

<a id="nestedblock--grants"></a>
### Nested Schema for `grants`

Required:

- `privileges` (List of String) The list of Snowflake privileges (e.g., SELECT, INSERT, USAGE). Use 'ALL' to grant all privileges for the resource type.
- `resource_type` (String) The type of Snowflake resource (database, schema, table, view, warehouse, dynamic_table, external_table, file_format, function, materialized_view, pipe, procedure, sequence, stage, stream, task)

Optional:

- `resource_names` (List of String) The names of the specific resources to grant on. Leave empty to grant on all resources of this type in the schema.
